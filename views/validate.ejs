<div class="card">
  <h2><i class="fas fa-shield-alt"></i> Validar enlace enviado</h2>
  <p>Utiliza esta herramienta para verificar la seguridad de los enlaces proporcionados en los reportes.</p>

  <form method="POST" action="/admin/validate" class="form-group">
    <div class="form-group">
      <label for="url"><i class="fas fa-link"></i> Introduce la URL a validar:</label>
      <input type="url" name="url" id="url" placeholder="https://ejemplo.com/imagen.jpg" required>
    </div>
    <button type="submit" class="btn"><i class="fas fa-check-circle"></i> Validar</button>
  </form>

  <% if (result) { %>
    <div style="margin-top: 2rem;">
      <h3><i class="fas fa-clipboard-check"></i> Resultado:</h3>
      <pre><%= result %></pre>
    </div>
  <% } %>
</div>

<div style="margin-top: 2rem;">
  <a href="/admin" class="btn"><i class="fas fa-arrow-left"></i> Volver al panel</a>
</div>
